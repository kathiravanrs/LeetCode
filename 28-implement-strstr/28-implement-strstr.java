class Solution {
    public int strStr(String haystack, String needle) {
        return haystack.indexOf(needle);
//         if(!haystack.contains(needle)){
//             return -1;
//         }
//         if(needle.length()==1){
//             return haystack.get
//         }
        
//         int c = 0;
//         while(haystack.substring(c).contains(needle)){
//             c++;
//         }
//         return c-1;
        
    }
}